# 하드웨어 및 개발환경 가이드

## 0. 보드 정보

| 항목 | 내용 |
|------|------|
| 모델 | NVIDIA Jetson Xavier NX Developer Kit |
| CPU | 6-core NVIDIA Carmel ARM v8.2 64-bit |
| GPU | 384-core NVIDIA Volta GPU + 2x NVDLA |
| RAM | 8GB LPDDR4x |
| JetPack | R32.6.1 (Ubuntu 18.04 aarch64) |
| Python | Python 3.6.9 (기본), pip3 설치 필요 |
| GPIO | Jetson.GPIO 라이브러리 (Jetson TX2/NX/AGX 공통 호환) |

## 1. NVIDIA Jetson Xavier NX 셋업

### 기본 연결
```
전원: 19V DC 배럴잭 또는 USB-C PD
디스플레이: LCD HDMI 연결
키보드/마우스: USB 무선 리시버
네트워크: Wi-Fi 또는 Ethernet
```

### 초기 설정 (사전 준비)
```bash
# 시스템 업데이트
sudo apt update && sudo apt upgrade -y

# Jetson GPIO 라이브러리 (Xavier NX 호환, 공식 라이브러리)
sudo pip3 install Jetson.GPIO

# I2C 도구
sudo apt install -y i2c-tools

# 필수 패키지
sudo apt install -y python3-pip python3-numpy python3-opencv
pip3 install smbus2 adafruit-circuitpython-dht
```

---

## 2. GPIO 핀맵 (Jetson Xavier NX 40-pin Header)

| 핀 번호 | 신호 | 연결 |
|---------|------|------|
| 1 | 3.3V | VCC |
| 2 | 5V | VCC |
| 6 | GND | GND |
| 11 | GPIO17 | 초음파 Trig 1 |
| 12 | GPIO18 | 초음파 Echo 1 |
| 13 | GPIO27 | 초음파 Trig 2 |
| 15 | GPIO22 | 초음파 Echo 2 |
| 16 | GPIO23 | IR 센서 1 |
| 18 | GPIO24 | IR 센서 2 |
| 22 | GPIO25 | IR 센서 3 |
| 27 | I2C SDA | LCD SDA |
| 28 | I2C SCL | LCD SCL |

> **주의**: 실제 핀맵은 AGX 보드 리비전에 따라 다를 수 있으므로 반드시 보드 문서 확인

---

## 3. 센서별 연결 및 코드

### 3.1 초음파 센서 HC-SR04

**연결**
| HC-SR04 | Jetson GPIO |
|---------|-------------|
| VCC | 5V (Pin 2 또는 4) |
| GND | GND |
| Trig | GPIO (Pin 11) |
| Echo | GPIO (Pin 12) - **반드시 저항 분배(5V→3.3V)** |

**Python 코드 예시**
```python
import Jetson.GPIO as GPIO
import time

TRIG = 17
ECHO = 18

def setup():
    GPIO.setmode(GPIO.BCM)
    GPIO.setup(TRIG, GPIO.OUT)
    GPIO.setup(ECHO, GPIO.IN)

def measure_distance():
    GPIO.output(TRIG, True)
    time.sleep(0.00001)
    GPIO.output(TRIG, False)
    
    pulse_start = time.time()
    pulse_end = time.time()
    
    while GPIO.input(ECHO) == 0:
        pulse_start = time.time()
    while GPIO.input(ECHO) == 1:
        pulse_end = time.time()
    
    pulse_duration = pulse_end - pulse_start
    distance = pulse_duration * 34300 / 2
    return distance

if __name__ == '__main__':
    setup()
    try:
        while True:
            dist = measure_distance()
            print(f"Distance: {dist:.1f} cm")
            time.sleep(0.5)
    except KeyboardInterrupt:
        GPIO.cleanup()
```

### 3.2 IR 근접센서

**연결**
| IR 센서 | Jetson GPIO |
|---------|-------------|
| VCC | 3.3V 또는 5V |
| GND | GND |
| OUT | GPIO (Pin 16) |

```python
IR_PIN = 23  # GPIO23

def setup_ir():
    GPIO.setup(IR_PIN, GPIO.IN)

def read_ir():
    return GPIO.input(IR_PIN)  # 0: 감지됨, 1: 없음

# 사용 예
if read_ir() == 0:
    print("장애물 감지!")
else:
    print("이상 없음")
```

### 3.3 LiDAR (RPLiDAR A1 / A2)

> JetPack R32.6.1 기준 ROS1 Melodic 사용. ROS2 사용 시 JetPack 업그레이드 필요.

```bash
# RPLiDAR 드라이버 설치 (ROS1 Melodic)
sudo apt install ros-melodic-rplidar-ros

# LiDAR 실행
roslaunch rplidar_ros rplidar.launch

# 토픽 확인 (다른 터미널)
rostopic echo /scan
```

**RVIZ로 시각화**
```bash
rviz
# Add → By topic → /scan → LaserScan
```

### 3.4 DHT11 습도 센서

```python
import Adafruit_DHT

sensor = Adafruit_DHT.DHT11
pin = 4  # GPIO4

def read_dht():
    humidity, temperature = Adafruit_DHT.read_retry(sensor, pin)
    return humidity, temperature
```

> Adafruit_DHT가 동작하지 않을 경우: `pip3 install adafruit-circuitpython-dht`

### 3.5 화재 감지 센서 (불꽃 센서)

```python
FLAME_PIN = 25  # GPIO25

def setup_flame():
    GPIO.setup(FLAME_PIN, GPIO.IN)

def detect_flame():
    return GPIO.input(FLAME_PIN) == 0  # 0: 불꽃 감지

# 사용 예
if detect_flame():
    print("⚠️ 화재 감지!")
```

---

## 4. 옴니휠 모터 제어

### 4.1 모터 드라이버 (L298N 또는 TB6612)

| 모터 드라이버 | Jetson GPIO |
|--------------|-------------|
| IN1 | GPIO5 |
| IN2 | GPIO6 |
| IN3 | GPIO13 |
| IN4 | GPIO19 |
| ENA (PWM) | GPIO12 (PWM) |
| ENB (PWM) | GPIO33 (PWM) |

### 4.2 옴니휠 3개 운동학

```
각 바퀴 120° 배치

v1 = -0.5*Vx + (√3/2)*Vy + R*ω
v2 = -0.5*Vx - (√3/2)*Vy + R*ω
v3 = Vx + R*ω

v1, v2, v3: 각 바퀴 속도
Vx, Vy: 로봇 전진/후진 속도
ω: 회전 각속도
R: 로봇 중심에서 바퀴까지 거리
```

```python
import math
import Jetson.GPIO as GPIO

# 모터 핀 설정
MOTOR_PINS = {
    'M1': {'in1': 5, 'in2': 6, 'pwm': 12},
    'M2': {'in1': 13, 'in2': 19, 'pwm': 33},
    'M3': {'in1': 20, 'in2': 21, 'pwm': 32},
}

def set_motor_speed(motor, speed):
    # speed: -100 ~ 100
    in1, in2, pwm = MOTOR_PINS[motor].values()
    
    if speed > 0:
        GPIO.output(in1, True)
        GPIO.output(in2, False)
    elif speed < 0:
        GPIO.output(in1, False)
        GPIO.output(in2, True)
    else:
        GPIO.output(in1, False)
        GPIO.output(in2, False)

    pwm_value = int(abs(speed) / 100 * 255)
    # PWM 출력 (하드웨어 PWM 또는 소프트웨어 PWM)

def omni_drive(vx, vy, omega):
    R = 0.15  # 로봇 반경 (m)
    sqrt3 = math.sqrt(3)
    
    v1 = -0.5*vx + (sqrt3/2)*vy + R*omega
    v2 = -0.5*vx - (sqrt3/2)*vy + R*omega
    v3 = vx + R*omega
    
    # -100~100 범위로 정규화
    max_v = max(abs(v1), abs(v2), abs(v3), 1)
    v1 = (v1 / max_v) * 100
    v2 = (v2 / max_v) * 100
    v3 = (v3 / max_v) * 100
    
    set_motor_speed('M1', v1)
    set_motor_speed('M2', v2)
    set_motor_speed('M3', v3)
```

---

## 5. LCD (I2C 1602 / 2004)

```python
import smbus2
import time

# I2C 주소 확인 (보통 0x27 또는 0x3F)
# sudo i2cdetect -y -r 1

class LCD:
    def __init__(self, addr=0x27):
        self.bus = smbus2.SMBus(1)
        self.addr = addr
        self.init_lcd()
    
    def write_text(self, text, line=0):
        # line 0: 첫째줄, line 1: 둘째줄
        self.set_cursor(line, 0)
        text = text.ljust(16)[:16]
        for char in text:
            self.write_char(ord(char))
    
    def display_sensor_data(self, dist, ir, temp, humid, flame_status):
        self.write_text(f"D:{dist:.0f}cm IR:{ir}", 0)
        self.write_text(f"F:{flame_status} T:{temp}C", 1)

# 사용 예
lcd = LCD(0x27)
lcd.display_sensor_data(45, 1, 25.5, 60, "OK")
```

---

## 6. ROS (Robot Operating System)

> **현재 보드 환경 (JetPack R32.6.1 / Ubuntu 18.04)**
> - ROS1 Melodic: 네이티브 지원 (apt install 가능)
> - ROS2 Eloquent: pip/소스 빌드 가능
> - **ROS2 Humble는 Ubuntu 22.04 필요** → JetPack 6.x 업그레이드 시 사용 가능
> - 교육 과정에서는 ROS1 Melodic을 기준으로 진행하며, ROS2 개념은 보조 설명

### ROS1 Melodic 설치
```bash
# ROS1 Melodic 설치 (Ubuntu 18.04)
sudo apt install -y ros-melodic-desktop

# 환경 설정
echo "source /opt/ros/melodic/setup.bash" >> ~/.bashrc
source ~/.bashrc

# 워크스페이스 생성
mkdir -p ~/catkin_ws/src
cd ~/catkin_ws
catkin_make
source devel/setup.bash
```

### ROS2 (향후 JetPack 업그레이드 시)
```bash
# ROS2 Humble (Ubuntu 22.04 / JetPack 6.x 필요)
# 워크스페이스 생성
mkdir -p ~/ros2_ws/src
cd ~/ros2_ws
colcon build
```

### 예제: 센서 데이터 발행 노드 (ROS1 Melodic)

```python
#!/usr/bin/env python3
import rospy
from std_msgs.msg import String
import Jetson.GPIO as GPIO

class SensorPublisher:
    def __init__(self):
        rospy.init_node('sensor_publisher')
        self.pub = rospy.Publisher('sensor_data', String, queue_size=10)
        self.rate = rospy.Rate(2)  # 2Hz
        GPIO.setmode(GPIO.BCM)
    
    def run(self):
        while not rospy.is_shutdown():
            msg = String()
            msg.data = f"distance: {self.measure():.1f}cm"
            self.pub.publish(msg)
            rospy.loginfo(f'Published: {msg.data}')
            self.rate.sleep()
    
    def measure(self):
        # 측정 로직...
        return 42.0

if __name__ == '__main__':
    try:
        node = SensorPublisher()
        node.run()
    except rospy.ROSInterruptException:
        pass
    finally:
        GPIO.cleanup()
```

---

## 7. 문제 해결 (Troubleshooting)

| 증상 | 원인 | 해결 |
|------|------|------|
| 초음파 값 0 or 튐 | Echo 핀 전압 불일치 | 5V→3.3V 레벨시프터 추가 |
| IR 센서 값 항상 0/1 | VCC/GND 잘못 연결 | 핀맵 재확인 |
| LiDAR 미동작 | USB 권한 | `sudo chmod 666 /dev/ttyUSB0` |
| I2C LCD 안나옴 | I2C 주소 틀림 | `i2cdetect -y -r 1`로 주소 확인 |
| 모터 안돌아감 | PWM 핀 비활성 | `sudo jetson_clocks` 실행 |
| ROS 노드 실행 안됨 | setup.bash 미실행 | `source /opt/ros/melodic/setup.bash` |

---

## 8. EasyEDA — 회로도/PCB 설계

### 8.1 시작하기

| 단계 | 내용 |
|------|------|
| 접속 | [EasyEDA](https://easyeda.com) — 회원가입 후 Editor 열기 |
| 에디터 모드 | Schematic(회로도) → PCB(기판) 순서로 작업 |
| 라이브러리 | 컴포넌트 검색: HC-SR04, Jetson GPIO Header, Resistor, LED |

### 8.2 회로도 작성 순서

```
1. 컴포넌트 배치
   - Jetson 40-pin Header: CONN-SMD 2x20 검색
   - HC-SR04: JST 커넥터 또는 핀헤더로 표현
   - 저항 분배 회로: 1kΩ + 2kΩ (Echo 5V→3.3V 레벨시프터)
   
2. 와이어 연결
   - VCC(5V) → HC-SR04 VCC
   - GND → HC-SR04 GND
   - GPIO17 → HC-SR04 Trig
   - GPIO18 → 저항분배 → HC-SR04 Echo
   - 네트워크 레이블(VCC, GND, TRIG, ECHO) 사용
   
3. DRC(Design Rule Check)
   - 연결 안 된 핀, 단락, 네트워크 오류 확인
   
4. 풋프린트 할당
   - 각 부품에 실제 PCB 풋프린트 매칭
   
5. PCB 레이아웃
   - 자동 라우팅(Auto Route) → 수동 미세 조정
   
6. 거버 출력
   - Fabrication Output → Gerber → ZIP 다운로드
```

### 8.3 학습용 템플릿

```text
[Schematic 기본 구조]
Jetson 40-pin Header (J1)
  Pin 1 (3.3V)  ──┬── VCC net
  Pin 2 (5V)    ──┘
  Pin 6 (GND)   ────── GND net
  Pin 11(GPIO17) ──── TRIG net ──┬── HC-SR04 Trig
  Pin 12(GPIO18) ──── ECHO net ──┼── 1kΩ ──┬── HC-SR04 Echo
                                  │           │
                                  └── 2kΩ ── GND (분배)
```

---

## 9. FreeCAD — 3D 모델링 (기구 설계)

### 9.1 설치 및 기본 설정

```bash
# FreeCAD 설치
sudo apt install freecad

# 또는 공식 사이트에서 AppImage 다운로드
# https://www.freecad.org/downloads.php
```

### 9.2 모델링 순서 (초음파 센서 브라켓 예제)

| 단계 | 내용 | FreeCAD 명령 |
|------|------|-------------|
| 1 | 새 문서 생성 | File → New → Part Design 워크벤치 |
| 2 | 스케치 평면 선택 | XZ 평면 (Sketch → Create Sketch) |
| 3 | 브라켓 베이스 스케치 | 사각형(30x40mm) + 모서리 필렛(R5) |
| 4 | 돌출 | Pad: 두께 5mm |
| 5 | 센서 장착 구멍 | Sketch on face → 원(Φ3mm, 4개) → Pocket |
| 6 | 센서 본체 모델링 | 별도 Body: HC-SR04 형상 단순화 (20x45x15mm) |
| 7 | 조립 | A2plus 워크벤치 → Import → Constraint |

### 9.3 3D 프린터 출력 준비

```text
1. Export → STL (메시 해상도: 0.1mm)
2. 슬라이싱 소프트웨어(Cura/PrusaSlicer)에서:
   - 레이어 높이: 0.2mm
   - Infill: 20%
   - 지지대: 필요 시 ON
3. G-code 생성 → 3D프린터로 출력
```

---

## 10. FEA 해석 (FreeCAD FEM)

### 10.1 해석 순서

| 단계 | 내용 |
|------|------|
| 1 | FEM 워크벤치로 전환 |
| 2 | 재료 할당: ABS 플라스틱 (Young's Modulus 2.3GPa, Poisson 0.35) |
| 3 | 고정 조건: 브라켓 나사 구멍 4개를 Fixed |
| 4 | 하중 조건: 센서 무게(2g) + 진동하중 (상단면에 1N 분포하중) |
| 5 | Meshing: Netgen → 2차 요소, 최대 크기 1mm |
| 6 | Run Solver → 결과 확인 (Von Mises Stress) |

### 10.2 해석 결과 해석

```
응력 분포 색상:
  ─ 파랑(낮음) → 초록 → 노랑 → 빨강(높음)

설계 개선 전: 응력 집중 (빨강) → 안전율 1.2
설계 개선 후: 리브 추가 + 두께 증가 → 안전율 3.5

"빨간 부분이 보이면 설계를 보강해야 한다"는 직관을 전달
```

### 10.3 FEA 따라하기용 치트시트

```python
# FreeCAD Python Macro (FEM 실행 자동화 예시)
import Fem
import FemGui
import ObjectsFem

# 재료 생성
material = ObjectsFem.makeMaterialSolid(FreeCAD.ActiveDocument)
material.Material = {
    'Name': 'ABS',
    'YoungsModulus': '2300 MPa',
    'PoissonRatio': '0.35',
    'Density': '1050 kg/m^3'
}

# 하중 조건 (1N 분포하중)
force = ObjectsFem.makeForceConstraint(FreeCAD.ActiveDocument)
force.Force = 1.0

# 실행
solver = ObjectsFem.makeSolverCalculix(FreeCAD.ActiveDocument)
solver.AnalysisType = 'static'
```

