# 06. ROS 2 로봇 통합

## 학습 목표

- ROS 2 아키텍처 이해 (노드, 토픽, 서비스)
- Jetson에 ROS 2 환경 구축
- 카메라/센서 노드와 로봇 제어 연동

## 사전 준비

- [ ] 04장 완료 (카메라, 센서)
- [ ] ROS 2 Humble 설치 (아래 참고)
- [ ] 호스트 PC(VirtualBox Ubuntu)에 ROS 2 준비

---

## 1. ROS 2 설치 (Jetson - Ubuntu 20.04)

```bash
$ sudo apt install ros-humble-desktop
$ echo "source /opt/ros/humble/setup.bash" >> ~/.bashrc
$ source ~/.bashrc
$ ros2 --help
```

> ※ ROS 2 Humble vs Jazzy 선택 기준, 설치 상세 자료 추가

---

## 2. 기본 개념

| 개념 | 설명 |
| ---- | ---- |
| Node | 실행 단위 프로그램 |
| Topic | Publish/Subscribe 통신 |
| Service | Request/Response 통신 |
| Message | 통신 데이터 타입 (예: Twist) |

```bash
$ ros2 node list
$ ros2 topic list
$ ros2 topic echo /cmd_vel
```

---

## 3. Publisher / Subscriber 실습

```python
# publisher (예제 골격)
import rclpy
from geometry_msgs.msg import Twist
from rclpy.node import Node

class CmdVelPublisher(Node):
    def __init__(self):
        super().__init__("cmd_vel_publisher")
        self.pub = self.create_publisher(Twist, "cmd_vel", 10)
```

> ※ Python 노드 작성 → `ros2 run`으로 실행하는 전체 코드는 교습 자료에 작성

---

## 4. 카메라/센서 노드

- USB 카메라 ROS 2 드라이버 (`usb_cam`)
- LiDAR/IMU 센서 드라이버 연동
- `rqt_graph`로 노드 간 연결 확인

```bash
$ ros2 run usb_cam usb_cam_node_exe
$ ros2 run rqt_graph rqt_graph
```

---

## 5. 로봇 통합

- `/cmd_vel`로 로봇(터틀봇/옴니휠) 주행 제어
- 객체 인식 결과 → 주행 명령 변환
- Jetson(로봇) ↔ 호스트 PC(VirtualBox) 네트워크 통신

> ※ 브리지 어댑터 설정, ROS_DOMAIN_ID 일치, 멀티캐스트 관련 주의사항 추가

---

## 실습

1. ROS 2 설치 및 Hello World 노드 실행
2. Publisher / Subscriber 통신
3. 카메라 영상을 ROS 토픽으로 발행
4. 키보드로 로봇 주행 제어

## 정리 및 체크리스트

- [ ] ROS 2 노드/토픽 개념 이해
- [ ] Publisher/Subscriber 코드 작성
- [ ] 카메라 토픽 발행
- [ ] 로봇 주행 제어
