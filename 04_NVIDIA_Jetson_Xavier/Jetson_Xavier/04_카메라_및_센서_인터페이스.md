# 04. 카메라 및 센서 인터페이스

## 학습 목표

- CSI / USB 카메라 영상 캡처
- OpenCV로 영상 처리
- 센서 데이터 실시간 수집

## 사전 준비

- [ ] USB 웹캠 또는 CSI 카메라 모듈
- [ ] OpenCV 설치

```bash
$ sudo apt install python3-opencv python3-pip
$ pip3 install opencv-python
```

---

## 1. USB 카메라 캡처

```python
import cv2

cap = cv2.VideoCapture(0)
while True:
    ret, frame = cap.read()
    if not ret:
        break
    cv2.imshow("camera", frame)
    if cv2.waitKey(1) & 0xFF == ord("q"):
        break

cap.release()
cv2.destroyAllWindows()
```

---

## 2. CSI 카메라 (GStreamer)

CSI 카메라는 GStreamer 파이프라인으로 접근합니다.

```bash
$ gst-launch-1.0 nvarguscamerasrc ! \
    'video/x-raw(memory:NVMM)' ! \
    nvvidconv ! autovideosink
```

> ※ NVIDIA 가속 파이프라인(`nvarguscamerasrc`) 사용 이유 설명 자료 추가

---

## 3. 영상 처리 기초 (OpenCV)

- 그레이스케일 변환
- 에지 검출 (Canny)
- 컬러 필터 (특정 색상 검출)

```python
import cv2

frame = cv2.imread("sample.jpg")
gray = cv2.cvtColor(frame, cv2.COLOR_BGR2GRAY)
edges = cv2.Canny(gray, 100, 200)
cv2.imshow("edges", edges)
cv2.waitKey(0)
```

---

## 4. 센서 데이터 수집

- IMU / LiDAR / 거리 센서 예제
- 센서 데이터를 CSV 또는 ROS 토픽으로 저장
- 타임스탬프 동기화 개념

---

## 실습

1. USB 카메라로 실시간 영상 확인
2. CSI 카메라 GStreamer 파이프라인 실행
3. OpenCV로 에지/컬러 검출
4. 센서 데이터 저장

## 정리 및 체크리스트

- [ ] USB/CSI 카메라 영상 캡처
- [ ] OpenCV 영상 처리
- [ ] 센서 데이터 수집 및 저장
